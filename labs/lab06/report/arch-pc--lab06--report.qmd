---
## Author
author:
  name: Сидорова А.А. 
  degrees: Студент
  student_card: 1032256488
  emai: 1032256488@pfur.ru
  affiliation:
    - name: Российский университет дружбы народов
      faculty: Факультет физико-математических и естественных наук
      department: Кафедра прикладной информатики и теории вероятностей
      city: Москва
      year: 2025

## Title
title: "**ОТЧЕТ ПО ЛАБОРАТОРНОЙ РАБОТЕ № 6**"
subtitle: "*дисциплина: Архитектура компьютера*"
student: "Студент: Сидорова А.А"
Group: "Группа: НБИбд -01-25"
---

# Цель работы 

Освоение арифметических инструкций языка ассемблера NASM.

# Задание для самостоятельной работы 

1. Написать программу вычисления выражения 𝑦 = 𝑓(𝑥). Программа должна выводить
выражение для вычисления, выводить запрос на ввод значения 𝑥, вычислять задан-
ное выражение в зависимости от введенного 𝑥, выводить результат вычислений. Вид
функции 𝑓(𝑥) выбрать из таблицы 6.3 вариантов заданий в соответствии с номером
полученным при выполнении лабораторной работы. Создайте исполняемый файл и
проверьте его работу для значений 𝑥1 и 𝑥2 из 6.3

Таблица 6.3. Выражения для 𝑓(𝑥) для задания №1
Номер варианта Выражение для 𝑓(𝑥) 𝑥1 𝑥2
1 (10 + 2𝑥)/3 1 10
2 (12𝑥 + 3)5 1 6
3 (2 + 𝑥)2 2 8
4 4
3 (𝑥 − 1) + 5 4 10
5 (9𝑥 − 8)/8 8 64
6 𝑥3/2 + 1 2 5
7 5(𝑥 − 1)2 3 5
8 (11 + 𝑥) ⋅ 2 − 6 1 9
9 10 + (31𝑥 − 5) 3 1
10 5(𝑥 + 18) − 28 2 3
11 10(𝑥 + 1) − 10 1 7
12 (8𝑥 − 6)/2 1 5
13 (8𝑥 + 6) ⋅ 10 1 4
14 ( 𝑥
2 + 8) ⋅ 3 1 4
15 (5 + 𝑥)2 − 3 5 1
16 (10𝑥 − 5)2 3 1
17 18(𝑥 + 1)/6 3 1
18 3(𝑥 + 10) − 20 1 5
19 ( 1
3 𝑥 + 5) ⋅ 7 3 9
20 𝑥3 ⋅ 1
3 + 21 1 3
При выполнении задания преобразовывать (упрощать) выражения для 𝑓(𝑥) нельзя. При
выполнении деления в качестве результата можно использовать только целую часть от
деления и не учитывать остаток (т.е. 5 ∶ 2 = 2)

# Теоретическое введение 
## Адресация в NASM
Большинство инструкций на языке ассемблера требуют обработки операндов. Адрес опе-
ранда предоставляет место, где хранятся данные, подлежащие обработке. Это могут быть
данные хранящиеся в регистре или в ячейке памяти. Далее рассмотрены все существующие
способы задания адреса хранения операндов – способы адресации.
Существует три основных способа адресации:
• Регистровая адресация – операнды хранятся в регистрах и в команде используются
имена этих регистров, например: mov ax,bx.
• Непосредственная адресация – значение операнда задается непосредственно в ко-
манде, Например: mov ax,2.
• Адресация памяти – операнд задает адрес в памяти. В команде указывается символи-
ческое обозначение ячейки памяти, над содержимым которой требуется выполнить
операцию
## Перевод символа числа в десятичную символьную запись
Ввод информации с клавиатуры и вывод её на экран осуществляется в символьном виде.
Кодирование этой информации производится согласно кодовой таблице символов ASCII.
ASCII – сокращение от American Standard Code for Information Interchange (Американский
стандартный код для обмена информацией). Согласно стандарту ASCII каждый символ
кодируется одним байтом.

# Порядок выполнения лабораторной работы
## 1.Символьные и численные данные в NASM
1. Создаю каталог для программ лабораторной работы №6 и перехожу в него. Внутри создаю файл lab6-1.asm (рис.1)

![рис.1](image/l61.png)

2. Ввожу в файл lab6-1.asm текст программы из листинга 6.1. В данной программе в ре-
гистр eax записываем символ 6 (mov eax,'6'), в регистр ebx символ 4 (mov ebx,'4').
Далее к значению в регистре eax я прибавла значение регистра ebx (add eax,ebx, ре-
зультат сложения записываем в регистр eax). Далее вывожу результат. Так как для работы
функции sprintLF в регистр eax должен быть записан адрес, необходимо я использую до-
полнительную переменную. Для этого записываю значение регистра eax в переменную buf1
(mov [buf1],eax), а затем записываю адрес переменной buf1 в регистр eax (mov eax,buf1) и
вызоваем функцию sprintLF. Создаю исполняемый файл и запускаю его. (рис.2-3)

![рис.2](image/l62.png)

![рис.3](image/l63.png)

3. Далее я изменила текст программы и вместо символов записала в регистры числа. Создала исполняемсый файл и запустила его. (рис.4-5)

![рис.4](image/l64.png)

![рис.5](image/l65.png)

Я получила символ, который не отображается при выводе.
4. Я создала файл lab6-2.asm в том же каталоге и ввела в него текст из программы листинга 6.2. Создаю исполняемый файл и запускаю его. (рис.6-7)

![рис.6](image/l66.png)

![рис.7](image/l67.png)
 
5. Аналогично третьему пункту я изменила текст файла  lab6-2.asm, создала исполняемый файл и запустила его. (рис.8-9)

![рис.8](image/l68.png)

![рис.9](image/l69.png)

## 2.Выполнение арифметических операций в NASM
6. Для примера я провела программу вычисления арифмитического выражения 𝑓(𝑥) = (5 ∗ 2 + 3)/3. Создаю файл lab6-3.asm в каталоге ~/work/arch-pc/lab06. Ввожу в этот файл данные из листинга 6.3. Создаю исполняемый файл и запускаю его. (рис.10-11)

![рис.10](image/l610.png)

![рис.11](image/l611.png)

Изменяю текст программы для вычисления выражения 𝑓(𝑥) = (4 ∗ 6 + 2)/5. Создаю исполняемый файл и проверяю его работу. (рис.12-13)

![рис.12](image/l612.png)

![рис.13](image/l613.png)

7. Создаю файл variant.asm в каталоге ~/work/arch-pc/lab06. Ввожу в файл текст листинга 6.4. Создаю исполняемый файл и запускаю его. (рис.14-15)

![рис.14](image/l614.png)

![рис.15](image/l615.png)

8.Вопросы
1. За вывод на экран сообщения ‘Ваш вариант:’ отвечают строки 
SECTION .data
msg: DB 'Введите № студенческого билета: ',0
rem: DB 'Ваш вариант: ',0

2.
mov ecx, x
mov edx, 80
call sread
Этот код готовит контекст для чтения строки, помещая её буфер (x) и максимальный размер (80) в регистры, чтобы функция sread могла корректно считать данные. 
3. Инструкция “call atoi”  позволяет легко конвертировать текстовое представление числа в формат, пригодный для арифметических вычислений.
4. За деление отвечают 
xor edx edx         
mov ebx 20          
div ebx             
inc edx 
5. Остаток записывается в регистр EDX.
6. inc edx эта инструкция увеличивает значение регистра EDX на единицу.
7.Эти строки отвечают за вывод на экран результата вычислений. 
mov eax,edx    ; Перемещение значения варианта (в edx после div) в eax
call iprintLF  ; Вывод числа варианта и перевод строки
call quit      ; Завершение программы

# Задание для самостоятельной работы 
 1. Создаю файл lab6-4.asm в каталоге ~/work/arch-pc/lab06. Ввожу туда программу для решения выражения 𝑓(𝑥) = 10 + (31𝑥 − 5) (вариант 9). Проверяю работу файла с x1 = 3, а также с x2 = 1 (рис.16-17)

![рис.16](image/l616.png)

![рис.17](image/l617.png)

Я самостоятельно посчитала значения и они совпали с теми, которые выдала программа. 

'Листинг 4.1. Программа для вычисления значения выражения 10+(31*x-5)'
; ---- Вычисление программы
%include 'in_out.asm' ; подключение внешнего файла
SECTION .data ; секция инициированных данных
msg: DB 'Введите значение переменной х: ',0
rem: DB 'Результат: ',0
SECTION .bss ; секция не инициированных данных
x: RESB 80 ; Переменная, значение к-рой будем вводить с клавиатуры
SECTION .text ; Код программы
GLOBAL _start ; Начало программы
_start: ; Точка входа в программу
; ---- Вычисление выражения
mov eax, msg ; запись адреса выводимиого сообщения в eax
call sprint ; вызов подпрограммы печати сообщения
mov ecx, x ; запись адреса переменной в ecx
mov edx, 80 ; запись длины вводимого значения в edx
call sread ; вызов подпрограммы ввода сообщения
mov eax,x ; вызов подпрограммы преобразования
call atoi ; ASCII кода в число, eax=x
mov ebx,31 ; EBX=31 
mul ebx ; EAX=EBX*EAX=31*x 
add eax,-5 ; EAX=EAX-5 
add eax,10 ; EAX=EAX+10
mov edi,eax ; запись результата вычисления в 'edi'

; ---- Вывод результата на экран
mov eax,rem ; вызов подпрограммы печати
call sprint ; сообщения 'Результат: '
mov eax,edi ; вызов подпрограммы печати значения
call iprintLF ; из 'edi' в виде символов
call quit ; вызов подпрограммы завершении

# Вывод 
При выполнении данной лабораторной работы я освоила арифметические
инструкции языка ассемблера NASM.

# Список литературы

1. Лабораторная работа №7
2. Таблица ASCII
